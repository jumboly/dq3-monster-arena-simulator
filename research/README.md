# Research sources

`rgh/` は RetroGameHackers「DQ3戦闘部分解説」シリーズのローカル写し（テキスト化）。著作物のため git 管理外。再取得は `scripts/fetch-research.sh`。

取得対象は `https://retrogamehackers.net/dq3-battlesystem-001/` 〜 `-026/`。README の Sources に挙げた補遺（`-011-2` 〜 `-011-4`）・改造記事・小ネタ集は対象外なので、必要ならブラウザで参照する。
